# Copiar este archivo como .env.local y completar los valores reales.
# .env.local NUNCA se commitea al repo.

# ── Server-side (Vercel env vars / funciones /api/*) ──────────────────────────
# En Vercel → Settings → Environment Variables (SIN prefijo VITE_, solo server-side):
ANTHROPIC_API_KEY=
ALPHA_VANTAGE_API_KEY=
